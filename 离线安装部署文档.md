# 试运行记录生成系统：离线安装部署

## 环境与首次安装

- Node.js 20 或 22（建议与打包机器相同的 Windows/Linux 及 CPU 架构）。
- 联网构建机进入项目目录运行 `npm ci`。
- Windows 离线包：运行 `npm run package:offline`，把 `dist/trial-record-generator-windows-offline.zip` 复制到目标机解压。
- 若离线包已包含 `node_modules`，目标机不需要访问 npm；若未包含，必须在联网机先执行 `npm ci` 后重新打包。

## 运行

- 前台调试：`npm start`
- Windows 后台启动：`scripts\start-background.bat`
- Windows 停止：`scripts\stop-background.bat`
- Linux：先执行 `chmod +x scripts/*.sh`，再运行 `./scripts/start-background.sh` / `./scripts/stop-background.sh`
- 浏览器访问 `http://127.0.0.1:3100`

如需局域网访问，把 `.env` 中 `HOST` 改为 `0.0.0.0`，并配置主机防火墙，仅向可信内网开放端口。

## 最终验证

Windows 运行 `scripts\verify-environment.bat`，Linux 运行 `./scripts/verify-environment.sh`。脚本检查 Node、锁文件、依赖、启停脚本、SQLite 目录，并在服务已启动时检查 `/healthz`。

数据位于 `data/app.db`，错误日志位于 `logs/error.log`；备份时复制数据库文件即可。
